[virtualenv]

# Standard isolated venvs - nothing global (dev tools included) gets
#   installed at the system level anymore, so there's nothing useful
#   for a venv to inherit here.
system-site-packages = false
